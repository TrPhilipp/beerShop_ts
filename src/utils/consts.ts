export enum ROUTES {
  HOME = '/',
  CATALOG = '/catalog',
  ABOUT = '/about',
}

export enum COLUMN_ID {
  BEER = 'beer',
  BASKET = 'basket',
}

export const BEER_URl = 'https://api.punkapi.com/v2/beers?per_page=80'

export enum BUTTON_LABEL {
  ADD_ALL = 'Add all',
  REMOVE_ALL = 'Remove all',
}

export enum COLORS {
  ITEM_DRAGGING = '#436e6d',
  ITEM_STANDART = '#69c2c0',
}

export enum BUTTON_ICON_CLASSNAMES {
  SHOPPING_CART = 'fas fa-shopping-cart',
  TRASH = 'fas fa-trash',
}
